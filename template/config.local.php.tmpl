<?php
/**
 * Local configuration — generated by seismo-generator for satellite {{SLUG}}.
 * Do NOT commit this file. Rebuild with seismo-generator to rotate keys.
 *
 * Two databases (same MySQL server, two names):
 *   DB_*              → this satellite’s LOCAL database: scores, system_config,
 *                       favourites — PHP connects here first; import sql/install.sql here.
 *   SEISMO_MOTHERSHIP_DB → main Seismo database that HOLDS ENTRIES (feeds, Lex, …).
 *                       Satellite READS only (GRANT SELECT). No sql/install.sql on mothership.
 */

// ── Satellite's own MySQL database (scores + local config — NOT the mothership DB) ──
define('DB_HOST', {{DB_HOST_PHP}});
define('DB_NAME', {{DB_NAME_PHP}});
define('DB_USER', {{DB_USER_PHP}});
define('DB_PASS', {{DB_PASS_PHP}});

// ── Mothership cross-DB reads (entries live here) ──────────────────
define('SEISMO_MOTHERSHIP_DB', {{MOTHERSHIP_DB_PHP}});

// ── Satellite mode switches ────────────────────────────────────────
define('SEISMO_SATELLITE_MODE', true);
define('SEISMO_MOTHERSHIP_URL', {{MOTHERSHIP_URL_PHP}});
define('SEISMO_REMOTE_REFRESH_KEY', {{REMOTE_REFRESH_KEY_PHP}});

// ── Branding ───────────────────────────────────────────────────────
define('SEISMO_BRAND_TITLE', {{BRAND_TITLE_PHP}});
define('SEISMO_BRAND_ACCENT', {{BRAND_ACCENT_PHP}});

// ── Standard settings ──────────────────────────────────────────────
define('DEFAULT_TIMEZONE', 'Europe/Zurich');
define('FEED_DIAGNOSTIC_KEY', '');
